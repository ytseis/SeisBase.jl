import Base: ==, -, *, +, append!, convert, copy, delete!, deleteat!,
  firstindex, getindex, hash, in, isempty, isequal, lastindex, length,
  push!, merge, merge!, merge_channels, read, setindex!, show, size, sizeof, sort!,
  sort, summary, write
import DSP: filtfilt
